package com.example.app.enums;

/**
 * Enum representing the role of a user in the system. It includes three roles: APPLICANT, OFFICER, and MANAGER.
 */
public enum Role {
    APPLICANT, OFFICER, MANAGER
}
